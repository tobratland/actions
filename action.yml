name: 'LLM Code Review'
description: 'Uses OpenAI GPT to review code changes in pull requests.'
inputs:
  OPENAI_API_KEY:
    description: 'OpenAI API Key'
    required: true
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.OPENAI_API_KEY }}